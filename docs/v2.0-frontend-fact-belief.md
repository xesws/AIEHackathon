# v2.0 — 前端交互组件:fact/belief/other 分流可视化(计划 + 结果)

> 承接 v1.9 分流重构(后端 + E2E proof 全绿)。本轮把分流落到前端交互:
> 让 demo 现场能"指着 prompt 说:belief 不在这里,但它答对了"。
> 范围:`frontend/src/engram.jsx`(+ rebuild bundle)。serving API 不改(`type` 已随
> `schema.to_dict` 透出,additive)。

## 0. 背景

v1.9 后:buffer 只装 **belief**(route==edit);rag_store 装 **fact + other**(都带 `type` 标签);
consolidated = 内化进权重的 belief。§0.3 的灵魂:**fact 在 prompt 里看得见(检索)、belief 在权重里看不见(但答对)**。

## 1. 三处交互改动

1. **buffer/Pending 区 → 重标为 belief**
   - 现文案"新事实 · 建议写入权重" → "信念/观点 · 内化进权重"。buffer 现在只有 belief。
2. **Reference/RAG store → 按 `type` 拆分展示 fact vs other**
   - `refresh()` 把 `m.type` 带到 refs;`MemorySurface` 把 fact(你的个人事实)与 other(参考资料)分组/分标签。
   - LabPanel 的 "reference · RAG store" 同样区分。
3. **★ proof 对比可视化(demo 主角)**
   - 答案来自 **RAG**(retrieved 非空)→ "retrieved · 在 prompt 里"(看得见);
   - 答案来自 **权重**(attribution.hit,rag_off)→ "from weights · 不在 prompt 里"(看不见但答对)。
   - 强化现有 TokenAttribution + retrieved badge,把"fact 显式 / belief 隐式"的对照讲清楚。

## 2. 护栏(must preserve)

- smoke(`tests/test_frontend_smoke.py`)要求的字符串全保留:API_HELPERS / ENDPOINTS /
  STATE_VARS(含 `buffer`/`weights`/`refs`)/ HANDLERS / COMPONENTS;无 localStorage/axios。
- 改完 **必须 `python frontend/build.py`** 重新内联 → index.html 与 source 同步(smoke 第 3 组断言)。
- 只动 `frontend/`;不碰 serving / memory / eval。targeted 编辑,保留现有配色与 polish,不重写整页。

## 3. 步骤

研究(并行:设计规格 + API 契约确认)→ 实现(改 jsx + build + smoke)→ 验证(独立跑 smoke + 同步检查 + 写结果)。

---

## 结果记录

### 改了哪些组件(`frontend/src/engram.jsx`)

1. **buffer/Pending 区 → 重标为 belief**
   - `LabPanel` 的 `staged · buffer` 文案改为 `信念/观点 · 待内化进权重`(L238);
   - `MemorySurface` 引导语 / Pending 标题 / commit 按钮 / weights 区副标题统一改为
     "信念/观点 · 内化进权重"(L345 / L356 / L372 / L400)。buffer 现在只装 belief。
2. **Reference/RAG store → 按 `type` 拆分 fact vs other**
   - `refresh()` 把 `m.type` 带进 refs(L603),reference 搜索结果同样带 `type`(L340);
   - `MemorySurface` 按 `r.type === "fact"` 渲染 `个人事实`(jade badge)/ `参考资料`(灰 badge)(L450–452);
   - `LabPanel` 的 `reference · RAG store` 同样区分(L261)。
3. **★ proof 对比可视化**
   - 消息气泡:RAG 命中 → `检索自你的文档 · 在 prompt 里(看得见)`(L502);
     权重命中(`attribution.hit && editOn`)→ `来自权重 · 不在 prompt 里(内化进模型,看不见却答对)`(L507)。
   - `LabPanel` signature 区加对照标题 `fact = 检索进 prompt(看得见) · belief = 内化进权重(不在 prompt,却答对)`(L286);
     `TokenAttribution` 命中尾注沿用 `codebook 注入 · 来自权重,不在 prompt 里`(L181)。

### build 产物 / smoke

- `python frontend/build.py` 重新内联:`app.bundle.js` 184,322 bytes、`index.html` 592,251 bytes。
  CJK 经 esbuild 转成大写 `\uXXXX` 转义(如 信念=`信念`),已在 bundle 与 index.html 中双向核对一致。
- `pytest tests/test_frontend_smoke.py -q`:**15 passed / 0 failed**(含 source↔index.html 同步断言)。
- 未触碰 `serving/ memory/ eval/ third_party/`,本轮仅前端三件套 + 本文档。

### proof-viz 现在怎么读

同一问题,fact 类答案带 `检索自你的文档 · 在 prompt 里(看得见)`——能指着 prompt 看到来源;
belief 类答案带 `来自权重 · 不在 prompt 里`——prompt 里没有它,模型却答对。
LabPanel 的对照标题把这条"fact 显式 / belief 隐式"的主张一句话讲清,现场可直接指给观众看。

### 真后端 demo 验证(`spikes/spike_v20_demo_fact_belief_http.py`,exit 0)

`uvicorn serving.app:app` 起真模型(~74s),走完整 hero loop over HTTP(浏览器 SPA 同源 serve):
- server_ready / **spa_serves**(`GET /` → 200 + `id="root"`)✅
- belief 教入 → buffer(type belief)→ `/consolidate` n_written=1 → buffer 抽干、edit_on=true ✅
- **★ PROOF**:`/chat` 问 belief、rag_off → 答 "Zarithon…"(`retrieved==[]`、`attribution.hit` sim=0.90)→ 来自权重 ✅
- **CONTRAST**:`/chat` 问 fact、rag_on → "Your cat's name is Coco."(`retrieved` n=4)→ 来自 RAG、在 prompt 里 ✅
- **热插拔**:`/edit-module {on:false}` 后再问 belief → 不再答 "Zarithon"(泛化回答)→ 证明答案在可插拔 adapter 里 ✅
- 路由(真 extract):`rag=[fact,fact,other]`、`buffer=[belief]`,INV-1/2 成立;`/rag/search?q=cat` 命中 Coco。

> 已知 wart(非阻塞):belief 的 rag_off 回答贪婪退化成 `Zarithonononon…`(edit 命中极强 + `use_cache=False`)。
> target 清晰命中、proof 成立,但浏览器现场观感欠佳。可选 follow-up:serving 的 `/chat` 解码加
> `repetition_penalty`/`no_repeat_ngram_size`(只动 decode 参数,不碰 editing/keying)。
