# v1.3 — token attribution(诚实·每条回答级)+ Reference 语义搜索

> 承接 v1.1(UI 接成真)。把功能清单里仍为 mock 的两处做成真:under the hood 的 **TokenAttribution** 与 Memory 页的
> **Reference 搜索框**。遵 CLAUDE.md 规则 #3/#4(开发前后写 /docs)。spec=本文 + 已批准计划。

## 为什么这么做(两条机制事实,已读码 verify)
1. **归因机制上是「每条回答一次检索决策」,不是逐 token。** live 解码 `use_cache=False` + `HOREN.generate` 把 `key_id`
   锁在 prompt 最后一个 token(`third_party/horen/.../editor.py`),所以每个生成 token 共享**同一个**
   `(命中槽 chosen_key, 相似度, 是否过 0.85 阈值)`。mock 的逐 token 彩虹机制上拿不到 → **诚实做成每条回答级**
   (不改解码、不追假逐 token)。
2. **codebook 槽 → MemoryItem 的映射当前不存在**(`PROV_EDIT_REF=id(adapter)` 对所有条目相同)。固化时
   `editing.edit` 返回的 `codebook_size` / `wrapper.edit_log["chosen_key"]` 被丢弃 → 本轮把 key index 记进 `provenance`
   (additive,数据现成)。
3. **搜索后端现成**:`memory/rag_store.search(q,k)->list[MemoryItem]`(embedding 召回 + LLM 重排,语义),`/chat` 已在用。

## 范围(用户已拍板)
- A=诚实每条回答级归因(真 槽/相似度/是否命中/←来自哪条记忆);B=语义搜索·提交触发。
- **不改** `generate.py` 解码 / **不改** `third_party/horen`(复用 `keying.compute_key`,它自带 forward-pre-hook)/ 不持久化 / 不碰 codebook reset(INV-S6)。

## 契约(新增 serving 面,字段名硬契约)
- `POST /chat`(扩展,additive)新增:
  `"attribution": null | { "hit": bool, "similarity": float, "threshold": float, "slot": int, "memory": null|{"id","text"} }`
  (`null` = editOn 关 / 无编辑 / 计算失败,best-effort 不破坏 /chat)。
- `GET /rag/search?q=<str>&k=5` → `{ "results": [item,...] }`(`item=schema.to_dict`,空 q→`[]`)。

## 改了什么(全 additive / 向后兼容)
**后端**
- `memory/schema.py`:`PROV_CODEBOOK_KEYS = "codebook_keys"`。
- `memory/consolidate.py` `_process_item`:固化成功后 best-effort 记
  `provenance[PROV_CODEBOOK_KEYS]={"native": ref["wrapper"].edit_log["chosen_key"], "chat": ref["codebook_size"]-1}`(try/except,绝不 break 固化)。
- `keying.py`:`gate(text,*,hf_model,tok,adapter)->(sim,slot)`——`compute_key`(chat query-span key)→ `adapter._query(rk)` 的 `max`/`argmax`。
  即 `eval/runtime.live_score` 再多取 argmax,**faithful math 留 keying 层**(INV-S1)。
- `serving/app.py`:模块级 `attribution(message)->dict|None`(`edit_active` 守卫 + `keying.gate` + 遍历 `store.by_status("consolidated")`
  的 `PROV_CODEBOOK_KEYS` 把 slot 映回记忆;整体 try/except→None)+ `/chat` 加 `attribution` 字段;模块级 `rag_search(q,k)` + `@app.get("/rag/search")`(**注册在 StaticFiles mount 前**)。

**前端 `frontend/src/engram.jsx`(+ `python frontend/build.py` 重建)**
- B:Reference 静态 `<span>` → 真 `<input>` + `apiRagSearch(q)`;回车/按钮触发 → loading → 展示「搜索结果·N 条」+ 清除 ✕;空查询回退默认 refs。
- A:`/chat.attribution` 挂到该条 assistant message → 传 LabPanel;**重写 `TokenAttribution`** 为诚实每条回答级:命中时整段答案统一淡绿底 +
  复用 callout「codebook hit · sim 0.91 ← memory: <真记忆>」+ readout `hit · sim>thr · slot #N` / `no hit(answered from base)`;
  editOn 关→「无归因」;删 mock `anchorTokens`。

## 不动
`generate.py`、`third_party/horen`、`editing.py`、`serving/store.py`/triggers stub、`memory/{store,buffer,rag_store,prompt}` 签名、
`CLAUDE.md`、`eval/*`、既有 `docs/v1.x`、`plan.md`。

## 测试 / 验证
- CPU 单测:`tests/test_serving_routes.py` 加 `/rag/search`(mock search)+ 归因 slot→item 映射(mock provenance);`tests/test_frontend_smoke.py` 补 `apiRagSearch`/`/rag/search`/attribution 接线。
- GPU(对运行中的 uvicorn,串行):`/rag/search` 语义结果 + 空 q→[];固化一条→`/chat` 相关问 `hit=true & sim>thr & memory` 指向那条、无关问 `hit=false`、editOn 关→`null`。先 N=1 sanity → STOP 报告。

## 护栏复核
不降级 torch;不改解码 / third_party;归因/搜索失败兜底回 null 不破坏 /chat;INV-S1/S3/S5/S6;只 `git add` 自己的文件。

## 结果

**CPU 单测(全绿)**
- `tests/` 全量 **159 passed**(基线 157 + 本轮新增 2 前端接线;serving 路由从 16→21)。
- `tests/test_serving_routes.py` 新增 5 例(纯 CPU,stub `keying.gate`/`model_host.edit_module`/`current_model`,无 GPU):
  - `/rag/search`:正常查询经 `rag_store.search(q,k)` 序列化为 `{results:[item]}` 且 `q`/`k` 透传;空白 `q` → `{results:[]}` 且**不**调 search。
  - 归因:`edit_active` 关 → `attribution=None`;命中(`sim=0.91`,slot 命中 `PROV_CODEBOOK_KEYS.chat`)→ `hit=true`、`memory` 指向该条、`threshold=0.85`、`slot` 正确;未过阈值 + 槽无主 → `hit=false`、`memory=null`。
- `tests/test_frontend_smoke.py`:15 passed(+`apiRagSearch`/`/rag/search` 进 wired 契约、index-sync 守卫、2 个 v1.3 接线断言:归因用真 `resp.attribution`/已删 mock `anchorTokens`、搜索框为真 `<input>`)。
- 重建产物已校验:`/rag/search`、`attribution`、`retrieval` 三处字符串均已 inline 进 `frontend/index.html` + `app.bundle.js`。
- torch 复核:装包前后均 `2.8.0+cu128 / True`(本轮未动依赖)。

**GPU 验证 —— pending(用户 2026-06-28 决定暂不做)**
- 决策当时 GPU 已被另一进程占 16GB(PID 2151488,疑似并发 backend session 常驻模型),为遵「单模型串行」未擅自再加载第二个模型。
- 待补(对运行中的 uvicorn,串行,N=1 sanity 先行 → STOP 报告):
  - `/rag/search?q=...` 回语义结果;空 q → `[]`。
  - 固化 1 条 → `/chat` 相关问:`attribution.hit=true & similarity>0.85 & memory` 指向那条;无关问:`hit=false`/`memory=null`;editOn 关:`attribution=null`。
  - 浏览器手测:搜索框真搜 + loading + 清除回退;under the hood 显示真记忆 + 相似度;关 edit module 归因消失。

**Commits**
- `ab8a296` v1.3 backend:per-answer codebook attribution + `/rag/search`(schema/consolidate/keying/app + 5 serving 单测)。
- `589077c` v1.3 frontend:真归因面板 + 语义 Reference 搜索(engram.jsx + 重建 bundle/index.html + 2 前端单测)。
